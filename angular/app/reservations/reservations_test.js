'use strict';

describe('myApp.reservations module', function() {

  beforeEach(module('myApp.reservations'));

  describe('reservations controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ReservationsCtrl = $controller('ReservationsCtrl');
      expect(ReservationsCtrl).toBeDefined();
    }));

  });
});